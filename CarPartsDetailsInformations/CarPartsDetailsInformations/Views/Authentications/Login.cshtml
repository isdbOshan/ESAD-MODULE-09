@model  CarPartsDetailsInformations.ViewModel.UserAuthentication.LoginViewModel
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>
<div class="row pt-4">
    <div class="col-12">
        <h3>Login</h3>
        @using (Html.BeginForm())
        {
            @Html.ValidationSummary()
            <div class="form-group row mb-1">
                @Html.LabelFor(x => x.UserName, new { @class = "col-form-label col-2 text-end" })
                <div class="col-4">
                    @Html.TextBoxFor(x => x.UserName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.UserName, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group row mb-1">
                @Html.LabelFor(x => x.Password, new { @class = "col-form-label col-2 text-end" })
                <div class="col-4">
                    @Html.PasswordFor(x => x.Password, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.Password, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group row mb-1">

                <div class="col-4 offset-2">
                    <button type="submit" class="btn btn-primary">Login</button>
                </div>
            </div>
            <div class="form-group row mb-1">

                <div class="col-12">
                    @Html.ActionLink("Register here", "Register", "Account")
                </div>
            </div>
        }
    </div>
</div>
